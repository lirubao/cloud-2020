server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lirubao/cloud-2020.git
          # 搜索目录
          search-paths:
            # - cloud-2020
            - cloud-config
          # 读取分支
      label: OpenFeign

eureka:
  client:
    service-url:
      defaultZone: http://eureka1:7001/eureka,http://eureka2:7002/eureka
